// dsh-approval-voice host half: browser-only plugin, no host-side behavior.
/** Host loader entry for the browser-only approval-voice plugin. */
/** Provides no host-side behavior. */
function apply() {}

export { apply };
